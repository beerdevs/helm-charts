apiVersion: v1
kind: Service
metadata:
  name: {{ include "azdo-app.fullname" . }}
  labels:
    {{- include "azdo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azdo-app.selectorLabels" . | nindent 4 }}