apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-flask-app.fullname" . }}
  labels:
    {{- include "simple-flask-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-flask-app.selectorLabels" . | nindent 4 }}